# Database
# For local development, use docker-compose: docker-compose up postgres
# For production, use managed PostgreSQL (e.g., Neon, Supabase, AWS RDS)
DATABASE_URL=postgresql://user:pg_password@localhost:5432/db

# Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth
# 1. Go to https://console.cloud.google.com
# 2. Create a project → APIs & Services → Credentials
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (dev)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Environment
# Options: local, development, staging, production
ENVIRONMENT=local

# Sentry (optional)
# Get from https://sentry.io → Create Project → Copy DSN
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Email Gateway
# Options: inMemory (dev/testing), resend (production)
EMAIL_GATEWAY=inMemory
# For production with Resend (https://resend.com):
# EMAIL_GATEWAY=resend
# EMAIL_RESEND_API_KEY=re_your-api-key-from-resend
# EMAIL_FROM=noreply@yourdomain.com

# Umami Analytics (optional)
# Get from your Umami instance or cloud.umami.is
# UMAMI_SCRIPT_URL=https://your-umami-instance.com/script.js
# UMAMI_WEBSITE_ID=your-website-id

# File Storage Gateway
# Options: inMemory (dev/testing), s3 (production)
FILE_GATEWAY=inMemory
# For production with S3-compatible storage:
# FILE_GATEWAY=s3
# S3_ENDPOINT=https://s3.amazonaws.com (or Cloudflare R2, MinIO, etc.)
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=your-access-key-id
# S3_SECRET_ACCESS_KEY=your-secret-access-key
# S3_PUBLIC_BUCKET=your-public-bucket-name
# S3_PRIVATE_BUCKET=your-private-bucket-name
# S3_PUBLIC_URL=https://your-cdn-url.com (for serving public files)
